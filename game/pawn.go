package game

func GetAvailablePositionForPawn(board *ChessBoard, startPosition Position) ([]Position, error) {
	// проверить, что пешка
	// обработка прямых ходов
	// обработка съедания
	return make([]Position, 0), nil
}
